/**
 * @deprecated Moved to @page-speed/forms/integration.
 *
 * Keep this file as a compatibility layer for existing imports inside
 * @opensite/ui blocks while the migration is in progress.
 */

export {
  generateInitialValues,
  generateValidationSchema,
  getColumnSpanClass,
  type FormFieldType,
  type FormFieldConfig,
  type SelectOption,
} from "@page-speed/forms/integration";
