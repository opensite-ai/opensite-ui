/**
 * GridFadeBottom - Individual export for tree-shaking
 *
 * A hero section with a grid pattern that fades from the bottom edge.
 * The grid lines are most visible at the bottom and gradually fade toward the top.
 * Ideal for sections that need visual grounding or when content is positioned above.
 *
 * @example
 * ```ts
 * import { GridFadeBottom } from "@opensite/ui/blocks/background-pattern-hero/grid-fade-bottom";
 * ```
 */

export { GridFadeBottom } from "../components/blocks/background-pattern-hero/grid-fade-bottom";
export type { GridFadeBottomProps } from "../components/blocks/background-pattern-hero/grid-fade-bottom";
