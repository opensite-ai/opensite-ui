/**
 * IframeEmbed - Individual export for tree-shaking
 *
 * Renders an owner-supplied third-party iframe embed (map, booking widget,
 * calendar, storefront, video host) inside the standard Section chrome, with
 * IntersectionObserver lazy loading and explicit containment control.
 *
 * @example
 * ```ts
 * import { IframeEmbed } from "@opensite/ui/blocks/advanced/iframe-embed";
 * ```
 */

export { IframeEmbed } from "../components/blocks/advanced/iframe-embed";
export type { IframeEmbedProps } from "../components/blocks/advanced/iframe-embed";
