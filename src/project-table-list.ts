/**
 * ProjectTableList - Individual export for tree-shaking
 *
 * A table-style project list with sortable columns. Displays projects in
 * rows with columns for title, category, year, and status. Includes hover
 * effects and optional sorting functionality. Perfect for portfolios that
 * need to display many projects in a compact format.
 *
 * @example
 * ```ts
 * import { ProjectTableList } from "@opensite/ui/blocks/project-list/project-table-list";
 * ```
 */

export { ProjectTableList } from "../components/blocks/project-list/project-table-list";
export type {
  ProjectTableListProps,
  ProjectTableListItem,
} from "../components/blocks/project-list/project-table-list";
