/**
 * ProjectMasonryColumns - Individual export for tree-shaking
 *
 * A masonry-style grid with varying card heights for visual interest. Cards
 * are distributed across columns with different aspect ratios creating a
 * Pinterest-like layout. Each card displays an image with hover overlay
 * showing title and category. Perfect for photography or design portfolios.
 *
 * @example
 * ```ts
 * import { ProjectMasonryColumns } from "@opensite/ui/blocks/project-list/project-masonry-columns";
 * ```
 */

export { ProjectMasonryColumns } from "../components/blocks/project-list/project-masonry-columns";
export type {
  ProjectMasonryColumnsProps,
  ProjectMasonryColumnsItem,
} from "../components/blocks/project-list/project-masonry-columns";
