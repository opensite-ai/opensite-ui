/**
 * ProcessHoverCards - Individual export for tree-shaking
 *
 * A process section with hover-activated cards that reveal floating images on desktop.
 * Each step displays a mono-font number, title, and description with smooth hover
 * transitions. The floating image appears with a fade-in animation when hovering.
 * Ideal for interactive process showcases and service overviews.
 *
 * @example
 * ```ts
 * import { ProcessHoverCards } from "@opensite/ui/blocks/process/process-hover-cards";
 * ```
 */

export { ProcessHoverCards } from "../components/blocks/process/process-hover-cards";
export type { ProcessHoverCardsProps } from "../components/blocks/process/process-hover-cards";
