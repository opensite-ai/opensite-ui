# Large Scale Architecture Strategy for Component Library

> This document outlines the architecture for how the component library will be structured in the future to allow for continually expanding the library without simultaneously expanding the NPM module's codebase size.
